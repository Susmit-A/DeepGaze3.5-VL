# These pins mirror the validated "vllm" conda environment used to train and
# evaluate this model. Python 3.12.11.
# Install into a fresh environment, or simply reuse the existing "vllm" env.
vllm==0.11.2
transformers==4.57.6
peft==0.18.1
torch==2.9.0
numpy==2.2.6
scipy==1.16.1
pillow==11.3.0
matplotlib==3.10.6
tqdm==4.67.3
accelerate==1.12.0
safetensors==0.7.0
